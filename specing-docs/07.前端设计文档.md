## 前端设计文档

### 修订记录

| 版本 | 日期 | 修订人 | 修订内容 |
|:---|:---|:---|:---|
| V1.0 | 2026-07-01 | — | 初始版本 |

---

### 一、技术栈

| 项目 | 选型 | 说明 |
|:---|:---|:---|
| 框架 | Vue 3 | 组合式 API + `<script setup>` |
| 语言 | TypeScript | strict 模式 |
| 构建 | Vite | 快速 HMR |
| UI 组件库 | Element Plus | Table / Form / Dialog / Tag / Menu 等 |
| 样式 | TailwindCSS | 仅用于间距和布局微调 |
| 状态管理 | Pinia | 组合式 store |
| 路由 | Vue Router 4 | Hash 模式 |
| HTTP 客户端 | Axios | 请求/响应拦截器 |
| 包管理 | pnpm | |

---

### 二、目录结构

```
frontend/
├── src/
│   ├── api/                   # API 请求层
│   │   ├── request.ts         # Axios 实例（拦截器、Token管理）
│   │   ├── auth.ts            # 登录相关
│   │   ├── contracts.ts       # 合同接口
│   │   ├── comparisons.ts     # 比对接口
│   │   ├── acceptance.ts      # 验收接口
│   │   ├── reports.ts         # 报表接口
│   │   ├── risks.ts           # 风险接口
│   │   └── dashboard.ts       # 仪表盘接口
│   ├── router/
│   │   └── index.ts           # 路由配置 + 导航守卫
│   ├── stores/
│   │   ├── auth.ts            # 认证状态
│   │   ├── contracts.ts       # 合同缓存
│   │   └── app.ts             # 全局状态（侧栏、面包屑）
│   ├── views/
│   │   ├── login/
│   │   │   └── LoginView.vue
│   │   ├── dashboard/
│   │   │   └── DashboardView.vue
│   │   ├── contracts/
│   │   │   ├── ContractList.vue
│   │   │   └── ContractDetail.vue
│   │   ├── comparisons/
│   │   │   └── ComparisonReview.vue
│   │   ├── acceptance/
│   │   │   └── AcceptanceManage.vue
│   │   ├── risks/
│   │   │   └── RiskDashboard.vue
│   │   └── reports/
│   │       └── ReportCenter.vue
│   ├── components/
│   │   ├── AppLayout.vue      # 主布局
│   │   ├── SideMenu.vue       # 侧栏菜单
│   │   ├── NavBar.vue         # 顶栏（面包屑+用户信息）
│   │   ├── DiffDisplay.vue    # 合同差异对比展示
│   │   ├── RiskTag.vue        # 风险等级标签
│   │   ├── StatusTag.vue      # 状态标签
│   │   └── Pagination.vue     # 分页封装
│   ├── types/
│   │   ├── api.ts             # 通用 API 类型
│   │   ├── contract.ts        # 合同相关类型
│   │   ├── comparison.ts      # 比对相关类型
│   │   ├── acceptance.ts      # 验收相关类型
│   │   ├── risk.ts            # 风险相关类型
│   │   └── dashboard.ts       # 仪表盘类型
│   ├── utils/
│   │   ├── format.ts          # 金额/日期/百分比格式化
│   │   └── export.ts          # Excel 导出
│   ├── styles/
│   │   └── global.css         # 全局样式覆盖
│   ├── App.vue
│   └── main.ts
├── index.html
├── vite.config.ts
├── tsconfig.json
├── tailwind.config.js
├── .env.development
├── .env.production
└── package.json
```

---

### 三、路由设计

#### 3.1 路由表

```typescript
const routes = [
  { path: '/login', name: 'Login', component: LoginView, meta: { public: true } },
  {
    path: '/',
    component: AppLayout,
    redirect: '/dashboard',
    children: [
      { path: 'dashboard',       name: 'Dashboard',       component: DashboardView },
      { path: 'contracts',       name: 'ContractList',    component: ContractList },
      { path: 'contracts/:id',   name: 'ContractDetail',  component: ContractDetail },
      { path: 'comparisons',     name: 'ComparisonReview', component: ComparisonReview },
      { path: 'acceptance',      name: 'AcceptanceManage', component: AcceptanceManage },
      { path: 'risks',           name: 'RiskDashboard',    component: RiskDashboard },
      { path: 'reports',         name: 'ReportCenter',     component: ReportCenter },
    ],
  },
]
```

#### 3.2 导航守卫

```typescript
router.beforeEach((to, from, next) => {
  const authStore = useAuthStore()
  if (!to.meta.public && !authStore.isLoggedIn) {
    next('/login')
  } else {
    next()
  }
})
```

#### 3.3 侧栏菜单结构

| 菜单 | 路由 | 图标 |
|:---|:---|:---|
| 仪表盘 | /dashboard | Odometer |
| 合同管理 | /contracts | Document |
| 比对审查 | /comparisons | DataAnalysis |
| 验收管理 | /acceptance | Clipboard |
| 风险看板 | /risks | Warning |
| 报表中心 | /reports | DataBoard |

---

### 四、Axios 封装

#### 4.1 request.ts

```typescript
import axios from 'axios'
import { ElMessage } from 'element-plus'

const request = axios.create({
  baseURL: import.meta.env.VITE_API_BASE || '/api/v1',
  timeout: 30000,
})

// 请求拦截器：自动附加 Token
request.interceptors.request.use((config) => {
  const token = localStorage.getItem('access_token')
  if (token) {
    config.headers.Authorization = `Bearer ${token}`
  }
  return config
})

// 响应拦截器：统一错误处理
request.interceptors.response.use(
  (response) => {
    const { code, message, data } = response.data
    if (code !== 0) {
      ElMessage.error(message)
      if (code === 4001) {
        localStorage.removeItem('access_token')
        window.location.href = '/#/login'
      }
      return Promise.reject(new Error(message))
    }
    return data
  },
  (error) => {
    ElMessage.error('网络错误')
    return Promise.reject(error)
  }
)
```

#### 4.2 API 类型定义

```typescript
// types/api.ts

export interface ApiResponse<T> {
  code: number
  message: string
  data: T
}

export interface PaginatedData<T> {
  items: T[]
  total: number
  page: number
  page_size: number
}

export interface PaginationParams {
  page?: number
  page_size?: number
  sort_by?: string
  sort_order?: 'asc' | 'desc'
}
```

---

### 五、状态管理（Pinia）

#### 5.1 auth store

```typescript
// stores/auth.ts
export const useAuthStore = defineStore('auth', () => {
  const token = ref(localStorage.getItem('access_token') || '')
  const adminInfo = ref<AdminInfo | null>(null)

  const isLoggedIn = computed(() => !!token.value)

  async function login(username: string, password: string) { ... }
  function logout() { ... }

  return { token, adminInfo, isLoggedIn, login, logout }
})
```

#### 5.2 app store

```typescript
// stores/app.ts
export const useAppStore = defineStore('app', () => {
  const sidebarCollapsed = ref(false)
  const breadcrumb = ref<string[]>([])

  function toggleSidebar() { ... }

  return { sidebarCollapsed, breadcrumb, toggleSidebar }
})
```

#### 5.3 contracts store

```typescript
// stores/contracts.ts
export const useContractStore = defineStore('contracts', () => {
  const list = ref<Contract[]>([])
  const total = ref(0)
  const loading = ref(false)

  async function fetchList(params: ContractListParams) { ... }

  return { list, total, loading, fetchList }
})
```

---

### 六、组件设计

#### 6.1 AppLayout.vue

主布局组件，结构如下：

```
┌─────────────────────────────────────────────────────┐
│  SideMenu          │  NavBar                        │
│                    │  面包屑 > 合同管理              │
│  ┌──── Dashboard   │  管理员张三  [退出]            │
│  │  合同管理       ├─────────────────────────────────┤
│  │  比对审查       │                                 │
│  │  验收管理       │      <router-view />            │
│  │  风险看板       │                                 │
│  │  报表中心       │                                 │
│                    │                                 │
└─────────────────────────────────────────────────────┘
```

- `SideMenu`：Element Plus `el-menu`，支持折叠
- `NavBar`：面包屑（`el-breadcrumb`）+ 用户信息 + 退出按钮
- 内容区域：`<router-view>` 渲染当前页面
- 响应式：小屏幕自动折叠侧栏

#### 6.2 DiffDisplay.vue

合同差异对比展示组件：

| 模式 | 说明 | 适用场景 |
|:---|:---|:---|
| 上下对比 | 前项在上，后项在下 | 单页合同快速比对 |
| 并排对比 | 左右两栏 | 条款级对比 |
| 逐行高亮 | 差异内容高亮标记 | 分项清单对标 |

Props：

```typescript
interface DiffDisplayProps {
  mode: 'stacked' | 'side-by-side' | 'line-by-line'
  frontContent: string | Record<string, any>
  backContent: string | Record<string, any>
  diffs?: DiffItem[]  // 可选，预计算的差异项
}
```

#### 6.3 RiskTag.vue

风险等级标签，根据等级显示不同颜色：

| 等级 | Element Plus Tag 类型 | 颜色 |
|:---|:---|:---|
| 高 | danger | 红色 |
| 中 | warning | 橙色 |
| 低 | info | 灰色 |
| 已忽略 | success | 绿色 |

#### 6.4 StatusTag.vue

状态标签：

| 状态 | 标签类型 | 颜色 |
|:---|:---|:---|
| 待确认 | warning | 橙色 |
| 已确认 | success | 绿色 |
| 比对完成 | primary | 蓝色 |
| 审查完成 | success | 绿色 |

---

### 七、页面设计

#### 7.1 登录页

- 居中卡片布局
- 用户名 + 密码输入框
- 登录按钮
- 错误的提示文字

#### 7.2 仪表盘

```
┌───────┐ ┌───────┐ ┌───────┐ ┌───────┐ ┌───────┐
│ 项目   │ │ 合同   │ │ 待确认 │ │ 高风险 │ │ 已完成 │
│  45    │ │  90    │ │   8   │ │   6   │ │  12   │
└───────┘ └───────┘ └───────┘ └───────┘ └───────┘
┌──────────────────────────────────────────────────┐
│                                                  │
│           各地市高风险数量分布（柱状图）              │
│                                                  │
└──────────────────────────────────────────────────┘
┌──────────────────────────────────────────────────┐
│                                                  │
│           高风险项目趋势（折线图）                    │
│                                                  │
└──────────────────────────────────────────────────┘
```

#### 7.3 合同列表

- `el-table` 展示合同列表
- 顶部筛选栏：合同编号搜索框 + 地市下拉 + 类型下拉 + 确认状态下拉
- 每行操作按钮：查看 / 编辑 / 确认 / 删除
- 分页组件 `el-pagination`

#### 7.4 合同详情

- `el-descriptions` 展示基本信息
- `el-collapse` 折叠关键条款

**布局**：

```
┌───────────────────────────────────────────────┐
│  合同详情 - HT-2024-001 前项                    │
├───────────────────────────────────────────────┤
│  ┌─ 基本信息 ──────────────────────────────┐  │
│  │  合同编号 │ HT-2024-001  │  甲方    │ .. │  │
│  │  签约日期 │ 2024-03-15  │  乙方    │ .. │  │
│  │  总金额   │ 5,280,000    │  类型    │ 前项│  │
│  └──────────────────────────────────────────┘  │
│  ┌─ 关键条款 ──────────────────────────────┐  │
│  │  > 付款条款                              │  │
│  │  > 交付条款                              │  │
│  │  > 验收条款                              │  │
│  │  > 违约责任                              │  │
│  └──────────────────────────────────────────┘  │
│  ┌─ 分项清单 ── [添加行] ──────────────────┐  │
│  │  # │ 名称   │ 数量  │ 单价   │ 金额      │  │
│  │  1 │ 土建    │ 1     │ 200万  │ 200万    │  │
│  └──────────────────────────────────────────┘  │
│                                                │
│   [确认无误]  [编辑修正]  [生成比对]            │
└───────────────────────────────────────────────┘
```

#### 7.5 比对详情

- 顶部展示金额对比卡片
- 中间 `DiffDisplay` 展示条款对比
- 底部 `el-table` 展示分项对比

```
┌───────────────────────────────────────────────┐
│  比对详情 - HT-2024-001                        │
├───────────────────────────────────────────────┤
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐       │
│  │ 前项  │ │ 后项  │ │ 差异  │ │毛利率│       │
│  │ 528万 │ │ 498万 │ │ 30万 │ │5.68%│       │
│  └──────┘ └──────┘ └──────┘ └──────┘       │
│                                               │
│  ┌─ 条款比对 ───────────────────────────┐   │
│  │                                      │   │
│  │ 付款条款 │ 一致 | 前项: ...          │   │
│  │          │ 后项: ...                 │   │
│  │          │ 差异详情: ...             │   │
│  │                                      │   │
│  │ 交付条款 │ 一致                      │   │
│  └──────────────────────────────────────┘   │
│                                               │
│  ┌─ 分项比对 ───────────────────────────┐   │
│  │  名称  │ 前项数量 │ 后项数量 │ 匹配    │   │
│  │  土建   │    1    │    1    │ 精确匹配 │   │
│  └──────────────────────────────────────┘   │
└───────────────────────────────────────────────┘
```

#### 7.6 风险看板

- 顶部 Tab 切换：全部风险 / 高风险 / 中风险 / 低风险
- `el-table` 展示风险列表
- 点击忽略按钮弹出确认对话框
- 右侧或底部汇总：各地市风险分布

#### 7.7 报表中心

- Tab 切换四个报表
- 每个报表分汇总表 + 明细表两个区域
- 导出 Excel 按钮

---

### 八、通用功能

#### 8.1 金额格式化

```typescript
// utils/format.ts
export function formatAmount(value: number): string {
  return new Intl.NumberFormat('zh-CN', {
    style: 'decimal',
    minimumFractionDigits: 2,
    maximumFractionDigits: 2,
  }).format(value)
}

export function formatPercent(value: number): string {
  return `${(value * 100).toFixed(2)}%`
}

export function formatDate(dateStr: string): string {
  return new Date(dateStr).toLocaleDateString('zh-CN')
}
```

#### 8.2 Excel 导出

```typescript
// utils/export.ts
import * as XLSX from 'xlsx'

export function exportToExcel(data: any[], fileName: string) {
  const worksheet = XLSX.utils.json_to_sheet(data)
  const workbook = XLSX.utils.book_new()
  XLSX.utils.book_append_sheet(workbook, worksheet, 'Sheet1')
  XLSX.writeFile(workbook, `${fileName}.xlsx`)
}
```

---

### 九、环境变量

| 变量 | 开发环境 | 生产环境 | 说明 |
|:---|:---|:---|:---|
| VITE_API_BASE | `/api/v1`（proxy） | `https://api.domain.com/api/v1` | API 地址 |
| VITE_APP_TITLE | 合同审查系统（开发） | 合同审查系统 | 页面标题 |

**vite.config.ts 代理配置**：

```typescript
export default defineConfig({
  server: {
    proxy: {
      '/api': {
        target: 'http://localhost:8000',
        changeOrigin: true,
      },
    },
  },
})
```
